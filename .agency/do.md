# /do config

## Check command

```bash
markdownlint **/*.md
```

## Format command

```bash
prettier --write **/*.md
```

> Note: `.prettierignore` excludes `.claude/` (generated by APM) and
> `apm_modules/` (vendored). Edit `.apm/instructions/` sources, then
> run `apm install` to regenerate `.claude/`. PHASE1-LEARNINGS:163
> records this gotcha biting twice before the exclusion was added.

## Test command

```bash
echo 'no tests yet'
```

## CI command

```bash
echo 'no CI yet'
```

## Documentation

Keep `README.md` in sync with user-facing changes.

<!-- Optional (add manually for the evidence step):
## PR evidence
-->
